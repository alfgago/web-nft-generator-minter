import styled from "styled-components"

import { COLORS, DEVICE } from "@/styles/variables"

export const EventListingStyles = styled.div``

export const ListingStyles = styled.section`
  .list {
    display: flex;
    flex-wrap: wrap;
    gap: 2rem;
  }

  .loading,
  .loadmore {
    text-align: center;
  }
`
