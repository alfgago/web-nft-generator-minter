import styled from "styled-components"
import { COLORS } from "@/styles/variables"

export const GuestsListStyles = styled.div`
  background-color: ${COLORS.white};
  padding: ${40 / 16}rem ${18 / 16}rem;
`
