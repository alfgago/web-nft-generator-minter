import styled from "styled-components"

import { COLORS, DEVICE } from "@/styles/variables"

export const TourStyles = styled.main`
  .star {
    position: absolute;
    right: 1.2rem;
    margin-top: -2.9rem;
  }
`
