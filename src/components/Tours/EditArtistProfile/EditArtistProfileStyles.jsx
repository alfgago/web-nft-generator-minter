import styled from "styled-components"

import { COLORS, DEVICE } from "@/styles/variables"

export const EditArtistProfileStyles = styled.div`
  padding-bottom: 2rem;
  .edit-profile-button {
    color: #fff;
    border-color: #fff;
    &:hover {
      background: rgba(255, 255, 255, 0.2);
    }
    .icon {
      margin-left: 0;
      margin-right: 1rem;
    }
  }
`
