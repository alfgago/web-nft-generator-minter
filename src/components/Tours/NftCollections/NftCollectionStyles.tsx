import styled from "styled-components"

import { COLORS, DEVICE } from "@/styles/variables"

export const NftCollectionStyles = styled.div`
  padding: ${40 / 16}rem ${18 / 16}rem;
  background-color: #ffffff;

  .header-collection {
    h1 {
      max-width: 21rem;
    }
  }
`
